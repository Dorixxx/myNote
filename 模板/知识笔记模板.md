<%*
const rawTitle = await tp.system.prompt("知识笔记标题");
const fallbackTitle = tp.file.title || "未命名";
const title = (rawTitle || fallbackTitle).trim();
const cleanedTitle = title.replace(/[\\/:*?"<>|]/g, "-").trim();
const fileTitle = cleanedTitle || ("未命名-" + tp.date.now("YYYYMMDD-HHmmss"));
const rawTopics = await tp.system.prompt("主题（多个主题用逗号分隔，可留空）");
const enteredTopics = (rawTopics || "")
  .split(/[,，]/)
  .map((item) => item.trim())
  .filter(Boolean);
const topics = [...new Set([].concat(enteredTopics))];
const source = (await tp.system.prompt("来源（AI、网址、书籍或个人记录，可留空）") || "").trim();
const date = tp.date.now("YYYY-MM-DD");
const yaml = (value) => JSON.stringify(value);
const topicsYaml = topics.length
  ? "topics:\n" + topics.map((item) => "  - " + yaml(item)).join("\n")
  : "topics: []";
await tp.file.move("知识库/知识/" + fileTitle);
tR += [
  "---",
  "type: knowledge",
  "status: active",
  topicsYaml,
  "source: " + (source ? yaml(source) : ""),
  "created: " + date,
  "updated: " + date,
  "---",
  "",
  "# " + title,
  "",
  "## 一句话结论",
  "",
  "## 适用场景",
  "",
  "## 关键内容",
  "",
  "## 注意事项",
  "",
  "## 相关笔记",
  "",
  "## 来源",
  ""
].join("\n");
%>
