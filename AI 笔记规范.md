---
type: reference
status: active
topics:
  - Obsidian
  - 知识管理
  - AI
source: AI
created: 2026-08-07
updated: 2026-08-07
---

# AI 笔记规范

本说明用于约束 AI 读取、创建和整理本 Vault 中的笔记。执行任务时，以本说明和用户当次指令为准。

## 基本原则

1. 先读取相关笔记，再提出分类、标题或修改建议。
2. 新生成内容默认写入 `AI 草稿/`。
3. 修改正式笔记前，先展示差异或修改摘要。
4. 删除、移动和批量修改必须获得用户单独确认。
5. 不把推测写成已经验证的事实。
6. 回答知识库问题时，尽量给出来源笔记路径。
7. 不永久删除内容，不清空回收站。
8. 操作后核对笔记数量、最终路径、关键正文和链接。

## 目录规则

| 目录 | 用途 |
|---|---|
| `收件箱/` | 临时记录和待整理内容 |
| `项目/` | 有明确结果或结束时间的事项 |
| `领域/` | 长期负责、持续关注的领域 |
| `知识库/知识/` | 概念、技术和经验总结 |
| `知识库/操作指南/` | 可重复执行的方法和流程 |
| `知识库/菜谱/` | 菜谱和实践记录 |
| `AI 草稿/` | AI 生成或整理后等待确认的内容 |
| `归档/` | 已结束、过时或暂不活跃的内容 |
| `模板/` | 笔记模板 |
| `附件/` | 图片和其他附件 |

目录表达用途和生命周期，不用目录穷举主题。目录层级最多三层；主题使用 `topics`、标题和双向链接表达。

## Properties 规范

正式笔记使用以下字段：

```yaml
---
type: knowledge
status: active
topics:
  - 主题
source:
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

`type` 可使用：

- `knowledge`
- `howto`
- `recipe`
- `project`
- `area`
- `reference`
- `plan`

`status` 可使用：

- `inbox`
- `active`
- `untested`
- `tested`
- `done`
- `archived`

`topics` 通常保留 1 到 3 个。AI 生成内容的 `source` 统一写为 `AI`；用户内容、项目资料和外部资料保留真实来源。

## 标题规则

- 标题包含以后可能搜索的关键词。
- 标题表达结论、问题或用途。
- 避免“记录一下”“一些技巧”“未命名”等弱标题。
- 文件名与一级标题尽量一致。
- 重命名前检查入链，完成后验证双向链接。

## 内容格式

知识笔记使用 [[模板/知识笔记模板|知识笔记模板]]。

操作方法使用 [[模板/操作方法模板|操作方法模板]]。

菜谱使用 [[模板/菜谱模板|菜谱模板]]。

不要为了套模板删除原始信息。短笔记可以保持简短，但应有清晰标题和最少必要的 Properties。

## AI 执行流程

1. 读取用户指定范围和相关链接。
2. 列出将要新增、修改、移动或删除的对象。
3. 对不确定分类标记为待确认。
4. 获得确认后执行批量变更。
5. AI 新内容写入 `AI 草稿/`，除非用户明确指定正式位置。
6. 修复因路径变化产生的链接。
7. 验证标题搜索、全文搜索、笔记数量和关键内容。
8. 输出变更摘要和仍需人工确认的事项。

## 安全边界

- 不未经确认覆盖长篇原文。
- 不删除唯一副本。
- 不清空回收站。
- 不暴露 Token、密码或其他凭据。
- 不把分享 Token 当作 MCP Token。
- 发现内容冲突时保留原文，并明确标出冲突。
