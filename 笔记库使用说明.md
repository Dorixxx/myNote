---
tags:
    - Obsidian
    - 知识管理
创建日期: "2026-08-07"
更新日期: "2026-08-07"
来源: AI
状态: 有效
类型: 操作方法
---

# 笔记库使用说明

这套笔记库以“快速记录、容易找到、整理成本低”为原则。平时不需要先想清楚所有分类，先记下来，再逐步整理。

## 从哪里开始

打开 [[知识库首页]]，可以进入项目、生活、菜谱、工作知识和 AI 草稿。

临时想法、待办线索和来不及分类的内容先放入 [[草稿/README|草稿]]。记录时优先写清楚标题和正文，不必为了属性打断思路。

## 笔记放在哪里

| 内容 | 目录 |
|---|---|
| 有明确目标、交付物或结束时间 | `项目/` |
| 长期负责或持续关注的事情 | `长期关注/` |
| 可以反复查阅的知识 | `知识库/知识/` |
| 可以照着执行的步骤 | `知识库/操作指南/` |
| 菜谱和烹饪实践 | `知识库/菜谱/` |
| 暂时不用但需要保留 | `归档/` |
| AI 生成、尚未确认 | `AI 草稿/` |

一个主题可能同时涉及项目和长期关注：具体交付放在项目，长期积累放在长期关注或知识库。

## 怎样写一篇笔记

1. 使用包含关键词的文件名。
2. 第一行写与文件名一致的一级标题。
3. 先写最重要的结论或事实。
4. 操作步骤使用有序列表。
5. 与现有内容有关时添加双向链接。
6. 来源明确时填写 `来源`。
7. 实践过的方法将状态从 `待验证` 改为 `已验证`。

可以直接使用：

- [[模板/知识笔记模板|知识笔记模板]]
- [[模板/操作方法模板|操作方法模板]]
- [[模板/菜谱模板|菜谱模板]]

使用模板后，要把 `来源` 改成这篇笔记的真实来源；由 AI 生成的内容写 `AI`。

## 怎样找到旧笔记

已知关键词时，先搜索标题或全文。

不记得准确关键词时：

1. 从 [[知识库首页]] 进入相关索引。
2. 查看当前笔记的双向链接。
3. 让 AI 搜索相关内容，并要求返回来源路径。

文件夹只负责大方向，不需要为每个小主题新建目录。

## 怎样和 AI 配合

适合交给 AI 的任务：

- 搜索相关笔记并列出路径。
- 总结指定笔记。
- 建议标题、属性和目录。
- 找出重复或相近内容。
- 生成索引、清单和整理草稿。

涉及移动、批量修改或删除时，应先让 AI 给出清单，再确认执行。AI 生成内容默认在 `AI 草稿/`，确认无误后再移入正式目录。

详细规则见 [[AI 笔记规范]]。

## 使用 Templater

### 首次设置

1. 打开“设置 → Templater”，将 **Template folder location** 设置为 `模板`。
2. 打开 **Trigger Templater on new file creation**。
3. 在 Templater 的 **Folder Templates** 中新增规则：文件夹选择 `草稿`，模板选择 `模板/基础笔记模板.md`。
4. 打开“设置 → 文件与链接”，把新建笔记的默认位置设为“下方指定的文件夹”，文件夹选择 `草稿`。

完成后，普通新建笔记会自动获得：

```yaml
---
类型:
状态: 待整理
tags: []
来源:
创建日期: 当天日期
更新日期: 当天日期
---
```

基础模板不会弹窗、不会重命名或移动文件，适合快速记录。Obsidian 核心功能本身不能为每篇新笔记自动注入这些属性，这一步由 Templater 的文件夹模板完成。

### 手动选择模板

从命令面板运行 **Templater: Create new note from template**。不要使用核心 Templates 插件的“插入模板”命令，否则脚本不会执行。

| 模板 | 自动移动到 | 初始状态 |
|---|---|---|
| 基础笔记模板 | 不移动 | `待整理` |
| 长期关注模板 | `长期关注/` 或指定子目录 | `有效` |
| 知识笔记模板 | `知识库/知识/` | `有效` |
| 操作方法模板 | `知识库/操作指南/` | `待验证` |
| 菜谱模板 | `知识库/菜谱/` | `待验证` |

长期关注模板会询问分类，例如 `宠物` 或 `工作/雪球`，并自动创建不存在的子目录。其他正式模板会询问标题、标签和来源。多个标签可以用中文逗号或英文逗号分隔。

正式模板的标题为必填项，留空或取消时不会移动笔记。标题中的文件名非法字符会替换为短横线；如果目标目录存在同名笔记，Obsidian 会阻止覆盖，需要换一个标题。

## 简单维护习惯

- 随手记录：放入草稿。
- 每周一次：处理草稿，更新进行中的项目。
- 项目结束：将结果沉淀到知识库，项目资料移入归档。
- 每月一次：检查备份能否恢复，并清理失效索引。
- 不直接删除不确定内容，先归档。
